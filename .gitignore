# MATLAB
*.asv
*.m~
*.mex*
*.mlx.autosave
slprj/
codegen/
*.slxc

# MATLAB Compiler / deployment output
*.exe
mccExcludedFiles.log
readme.txt
requiredMCRProducts.txt
*.prj.autosave

# Local project instructions (not published)
CLAUDE.md

# Local research notes (not published)
compass_artifact_*.md

# Example script output
matlab/examples/bulk_results.xlsx

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Reference standards and vendor documents (not redistributable).
# They live in references/ — the folder is ignored wholesale so a new
# document dropped in never needs a .gitignore edit to stay unpublished.
references/
*.pdf
