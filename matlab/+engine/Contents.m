% +ENGINE  Analysis math — the core (GUI-independent, headless-capable).
%
%   Will hold: preload (incl. thermal), bolt/member stiffness + stiffness
%   factor, applied-load resolution, the 15 margin checks, interaction
%   (5020A Eq. 20-23), separation/slip, tapped-hole parent-thread check,
%   and the single-joint solver.
%
%   Reference for structure: MATLAB_BUILD_GUIDE.md, Track A.
%   Currently empty — first real code lands at A2 (data model).
