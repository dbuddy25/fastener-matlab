function fastenerTool()
%FASTENERTOOL  Entry point for the MATLAB fastener analysis tool.
%
%   A1 STUB — proves the project is set up and runs. No analysis yet.
%   Later milestones replace this with real launch/analysis behavior
%   (E1 formalizes version/build stamping).
%
%   Usage (from the matlab/ folder, or with matlab/ on the path):
%       fastenerTool

    v = "0.1.0";   % new tool starts fresh (Python tool is a separate lineage)
    fprintf("Fastener Analysis Tool (MATLAB) v%s\n", v);
    fprintf("  Stub entry point — engine not built yet (milestone A1).\n");
end
